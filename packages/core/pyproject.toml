[project]
name = "alias-app-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Adrià Pérez Alejandre", email = "perezadria00@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
alias-app-core = "alias_app_core:main"

[build-system]
requires = ["uv_build>=0.10.3,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
